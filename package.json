{
  "name": "execution-gap-cli",
  "version": "0.1.0",
  "private": true,
  "description": "Command-line analyzer for Ethereum transaction execution gaps (mempool simulation vs actual inclusion).",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "tx:run": "tsx cli/tx-run.ts",
    "wallet:run": "tsx cli/wallet-run.ts",
    "protocol:run": "tsx cli/protocol-run.ts",
    "lint": "eslint",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "axios": "^1.15.0",
    "dotenv": "^17.4.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^20",
    "eslint": "^9",
    "tsx": "^4.21.0",
    "typescript": "^5"
  }
}
